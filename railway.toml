[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "su node -s /bin/sh -c 'node --import /app/server/node_modules/tsx/dist/loader.mjs /app/server/dist/index.js'"
healthcheckPath = "/api/health"
