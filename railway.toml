[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "su node -s /bin/sh -c 'node --import ./server/node_modules/tsx/dist/loader.mjs server/dist/index.js'"
healthcheckPath = "/api/health"
